import React from "react";

const SocialMedia = (props) => {
  return (
    <div>
      <img
        style={{ width: "25%" }}
        src={require("../images/iphoneBezel.png")}
      />
    </div>
  );
};

export default SocialMedia;
